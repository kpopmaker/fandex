{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_REF\" in validation/*) echo 'Skipping Vercel build for validation branch'; exit 0 ;; *) exit 1 ;; esac"
}
