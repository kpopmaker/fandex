# v106 candidate-approval decision-application preview

This bounded sandbox stage applies the exact validator-permitted v105 `approve_candidate` shadow submission to the exact ready v104 packet through the public pure `v67.simulate` copied-state boundary and its v66 contract.

The result distinguishes a derived preview approval from persistent state: the candidate is approved only in deterministic copied-state output. No adapter, database, queue, historical-request store, normalized record, provider, network, or production boundary is invoked. Replay returns the same application identity as an idempotent existing result without a duplicate effect.

The historical v97 `request_enrichment` decision remains immutable lineage and is not reapplied. The ignored enrichment PNG and all predecessor inputs remain unchanged and uncommitted.
