# AESPA human re-review decision submission v86 preview

V86 consumes the merged v85 corrected public decision input as its only runtime context. V85 conformance is passed, submission readiness is ready, and its actual dependency registry remains closed at 31 entries: 23 public, three tracked immutable authorities, five future explicit-human inputs, zero unresolved, and zero private-state dependencies. V86 does not access v85/v84/v83/v82/v81 private state.

Immutable semantic authority remains `human_review_decision_contract.preview.json`, `human_review_decision_application_contract.preview.json`, and the directly imported `validate_human_review_decisions.py::validate_entry`. Gate status comes from the v85 public `gate_status` field and is never inferred from `gate_id`. The canonical ordered vocabulary is `not_decided`, `approve_candidate`, `accept_exception`, `reject`, `defer`, and `request_enrichment`. `not_decided` is the blank placeholder and cannot become a submitted decision.

The exact v85 future inputs are `selected_decision`, `reviewer`, `reviewed_at`, `rationale_codes`, and `requested_enrichment_fields`. V86 supplies deterministic controlled fixtures, including reviewer `controlled-v86-human-reviewer` and timestamp `2030-01-02T03:04:05Z`. The tracked schema has no actor/origin field, so explicit human provenance is represented by the controlled-human-input source class; V86 invents no field. Historical reviewer `jm-reviewer-001`, its timestamp, and historical rationale are not copied.

Every canonical intent is passed through the real validator. At the authoritative `exception_review_required` gate, `approve_candidate` is rejected; `accept_exception`, `reject`, `defer`, and `request_enrichment` are permitted and produce detached shadow submissions. A validator rejection is a valid fail-closed matrix result and emits no submission. The shadow ID is local-only SHA-256 over canonical JSON binding, in order, decision input, packet, request, target, gate identity/status, controlled decision and reviewer metadata. It is not an application, audit, persistence, or historical identifier.

Exact v85 public lineage is preserved. The v85 blank object stays `not_decided`, unsubmitted, unpreselected, with null reviewer/time and empty rationale. Historical `request_enrichment` stays unchanged. Two fresh v85/v86 runs, insertion-order checks, detached-copy mutation checks, authority hashes, Last.fm hashes, and all generated JSON are verified. Network, database, semantic persistence, submission, application, audit, queue, ranking, public-data, and production effect counters remain zero.

V86 does not choose the real AESPA review decision. V86 does not change historical `request_enrichment`. V86 does not submit a decision to production. V86 does not apply `approve_candidate`, `accept_exception`, `reject`, `defer`, or `request_enrichment`. V86 treats decision intents only as controlled validation data. It performs no automatic selection and makes no recommendation.

The ready boundary is a separate v87 decision-application preview consuming a validated v86 shadow submission. V87 is not implemented here and must still stop before every real write, mutation, persistence, audit, queue, or production effect.
