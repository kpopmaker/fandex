# AESPA authorized external evidence submission preview (v91)

v91 consumes the public v90 intake boundary and validates two separately bound evidence submissions through the public v81 validator exposed by v90. The exact historical request is `4788f3059b8b0a5b111aafd475c1ff3a6fa47dc60be690236a2603001735f283`; its decision remains `request_enrichment`, reviewer `jm-reviewer-001`, and requested fields remain `content_context` and `source_attribution`.

Raw row 991 uniquely binds `src_40f253cea60253b4f7b8d1e747f9cc87`, the MyDaily origin locator, and Naver provider tuple `117/0004076125`. The selected archive is verified byte-identical. The content-context candidate uses the exact target-bound normalized title plus its 121-code-point NFC summary. The source-attribution candidate uses explicitly displayed `마이데일리` with semantic role `publisher`, supplied through a project-owner human browser observation. No author is claimed.

The ignored screenshot is bound by SHA-256 `57c1f7fd0e5b83734c672186bbac61646dd1c723984a14522876326fa0efe2b8` and dimensions 1783 × 851; no PNG bytes are tracked. No full article body is retained. The earlier automated MyDaily and Naver history remains two attempts and zero successes. v91 performs no network read and records one human evidence submission.

Both outputs are only `authorized_external_evidence_submitted_as_unverified_intake_candidate`. Structural submission is not external verification, acceptance, field satisfaction, historical fulfillment, human-review readiness, historical authority, or production authority. v91 never calls acceptance or completion evaluation and performs no database, source, queue, audit, ranking, chart, public-data, or production mutation.

The next safe stage is a separate external-verification-boundary preview. Acceptance, satisfaction, fulfillment, human review, decision capture/submission, and application remain blocked. Production persistence and execution remain not ready. No PR or merge is performed.
