name: PR127 momentum v181 capability recheck 20260925

on:
  push:
    branches:
      - v131-lastfm-real-signal-read-model
    paths:
      - data/momentum-research/iu_verifier_external_capability_resume_gate_v181_recheck_20260925T082224KST.json
      - tests/fandex-momentum-verifier-external-capability-resume-gate-v181.test.mts
      - .github/workflows/pr127-momentum-v181-capability-recheck-20260925.yml

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: v181 tests
        run: npx tsx --test tests/fandex-momentum-verifier-external-capability-resume-gate-v181.test.mts
      - name: v180 regression
        run: npx tsx --test tests/fandex-momentum-verifier-external-execution-channel-resolution-v180.test.mts
      - name: typecheck
        run: npm run typecheck
      - name: branch diff check
        run: git diff --check HEAD^..HEAD
      - name: full PR diff check
        run: git diff --check origin/main...HEAD
