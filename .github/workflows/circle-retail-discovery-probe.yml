name: Circle Retail Discovery Validation

on:
  push:
    branches:
      - integration/album-circle-retail-discovery

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run Circle retail discovery tests
        run: npx tsx --test tests/circle-retail-discovery-v1.test.mts
      - name: Typecheck
        run: npm run typecheck
