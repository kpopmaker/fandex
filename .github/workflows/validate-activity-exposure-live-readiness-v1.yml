name: Validate Activity Exposure live readiness v1

on:
  push:
    branches:
      - validation/activity-exposure-live-readiness-v1

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FANDEX_RUNTIME_DATABASE_URL: ${{ secrets.FANDEX_RUNTIME_DATABASE_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Read-only Neon readiness
        run: npx tsx scripts/validation/activityExposureLiveReadinessV1.ts
