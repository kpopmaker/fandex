name: PR170 IU Luminate stored normalization validation

on:
  push:
    branches:
      - research/musicbrainz-album-catalog-v1

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Validate stored observation to normalization path
        run: |
          npx tsx --test tests/iu-luminate-stored-normalization-research-v1.test.mts
          npx tsx --test tests/iu-luminate-baseline-acquisition-research-v1.test.mts
          npx tsx --test tests/luminate-album-observation-intake-research-v1.test.mts
          npx tsx --test tests/luminate-album-observation-persistence-research-v1.test.mts
          npx tsx --test tests/album-normalization-research-v1.test.mts
          npx tsx --test tests/album-normalization-readiness-integration-v1.test.mts
          npm run typecheck
